version: '2'
services:
  lampaderum-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/Lampaderum/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=Lampaderum
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
